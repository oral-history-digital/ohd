class SetSpeakerIdsOnMogSegments < ActiveRecord::Migration[5.2]
  def change
    #if Project.first.initials == 'mog'
      #Interview.all.each do |i| 
        #i.tapes.each do |t| 
          #t.segments.each do |s|
            #if s.speaker_id.nil?
              #s.update_attributes(speaker_id: s.prev.speaker_id) if s.prev && s.prev.speaker_id
            #end
          #end
        #end
      #end
    #end
  end
end
