import { NAME } from './constants';

export const REQUEST_REGISTRY_TREE = `${NAME}/request`;
export const RECEIVE_REGISTRY_TREE = `${NAME}/receive`;
export const ERROR_REGISTRY_TREE = `${NAME}/error`;
