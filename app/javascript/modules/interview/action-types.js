import { NAME } from './constants';

export const SET_INTERVIEW_TAB_INDEX = `${NAME}/setTabIndex`;
export const ENABLE_AUTO_SCROLL = `${NAME}/enableAutoScroll`;
export const DISABLE_AUTO_SCROLL = `${NAME}/disableAutoScroll`;
