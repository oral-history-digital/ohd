<% cache [ I18n.locale.to_s, @interview.to_param, 'headings'].join('/') do -%>

  <% headings_hash = hashed_headings_from_array(@interview.segments.with_heading || []) %>
  <%= (@player ||= interview_player(@interview)).headings_container(headings_hash) %>

<% end %><%# cache %>
