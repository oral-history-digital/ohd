<h2 class="info">Service-Funktionen</h2>
<p>Suche im Archiv</p>
<div id="baseHeaderSearch">

  <span class="hits">
    <% if search.hits == Interview::NUMBER_OF_INTERVIEWS %>
      <%= link_to t(:all) + '&nbsp;Interviews', current_search_path %>
    <% elsif search.hits > 0 %>
      <%= link_to pluralize(search.hits, Interview.human_name) + '&nbsp;in&nbsp;der&nbsp;Auswahl', current_search_path %>
    <% else %>
      keine&nbsp;Interviews&nbsp;in&nbsp;der&nbsp;Auswahl
    <% end %>
  </span>
</div>

<div id="back_to_search" class="action">
  <%= link_to image_tag(image_path('suchpfeil_zurueck.gif'), :title => 'zur Suche'), current_search_path %>
</div>

<div id="search_header_overlay" class="overlay" style="display: none;"></div>