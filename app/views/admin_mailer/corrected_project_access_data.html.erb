<%
  case I18n.locale.to_sym
  when :en
-%>
  <p>Hello,</p>
  <p>There is a corrected request for activation from user '<%= @user_name %>' in application '<%= @project.name(@project.default_locale) %>'.</p>
  <p>Please review it as soon as possible.</p>
  <p><%= @url %></p>
  <p>Best regards</p>
  <p><%= @project_name %></p>
<% else -%>
  <p>Guten Tag,</p>
  <p>in der Anwendung <%= @project.name(@project.default_locale) %> liegt eine benutzerseitig (<%= @user_name %>) korrigierte Anfrage auf Freischaltung vor.</p>
  <p>Bitte überprüfen Sie diese so schnell wie möglich.</p>
  <p><%= @url %></p>
  <p>Viele Grüße</p>
  <p><%= @project_name %></p>
<% end -%>
