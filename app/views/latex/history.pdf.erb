\title{<%= @interview.full_title(I18n.locale) %>}
\begin{document}
\maketitle

<% @interview.interviewees.first.histories.each do |history|  %>

    <%= history.send(@language[:type].pluralize.to_sym).where(locale: @language[:locale]).first.deportation_date %>
    <%= history.send(@language[:type].pluralize.to_sym).where(locale: @language[:locale]).first.return_date %>
    <%= history.send(@language[:type].pluralize.to_sym).where(locale: @language[:locale]).first.forced_labor_details %>

<% end %>



