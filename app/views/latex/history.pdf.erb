\title{<%= @interview.full_title(I18n.locale) %>}
\begin{document}
\maketitle

<% @interview.interviewees.first.histories.each do |history|  %>

    <%= latex_escape(history.translations.where(locale: @language[:locale]).first.deportation_date) %>
    <%= latex_escape(history.translations.where(locale: @language[:locale]).first.return_date) %>
    <%= latex_escape(history.translations.where(locale: @language[:locale]).first.forced_labor_details) %>

<% end %>



