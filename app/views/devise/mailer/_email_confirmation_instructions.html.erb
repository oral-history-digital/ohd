<% url = "#{@resource.user_registration.projects.last.archive_domain}/#{I18n.locale}/accounts/#{@resource.id}/confirm_new_email?confirmation_token=#{@resource.confirmation_token}" %>
<% case I18n.locale
  when :en -%>
    <p>Welcome <%= @resource.display_name %>!</p>

    <p>You can confirm your new email now through the link below:</p>

    <p><%= link_to 'Confirm email', url %></p>
  <% when :ru -%>
    <p>Добро пожаловать <%= @resource.display_name %>!</p>

    <p>Вы можете подтвердить Ваш доступ, перейдя по следующей ссылке:</p>

    <p><%= link_to 'Подтвердить доступ сейчас', url %></p>
  <% when :el -%>
        <p>Καλώς ήρθατε <%= @resource.display_name %>!</p>
        <p>Μπορείτε να επιβεβαιώσετε την είσοδό σας πατώντας στο παρακάτω λινκ:</p>

        <p><%= link_to 'Zugang jetzt bestätigen', url %></p>
  <% when :es -%>
  <p>Estimada/o <%= @user_account.display_name %>,</p>
  <p>Usted ha cambiado el correo electrónico de su cuenta para la aplicación "<%= @user_account.user_registration.projects.last.name('es') %>".</p>
  <p>Por favor, confirme los cambios haciendo clic en el siguiente enlace:</p>
  <p><%= link_to 'Confirmar nuevo correo electrónico', url %></p>
  <p>Con saludos cordiales,</p>
  <p>El equipo del proyecto "<%= @user_account.user_registration.projects.last.name('es') %>"</p>
  <% else -%>
    <p>Willkommen <%= @resource.display_name %>!</p>

    <p>Sie können Ihre neue Email jetzt bestätigen unter dem nachfolgenden Link:</p>

    <p><%= link_to 'Email jetzt bestätigen', url %></p>
<% end -%>
