<%
  my_locale = @user.default_locale || I18n.locale
  if @resource.active_projects.count == 1
    project = @resource.active_projects.last
    project_name = project.name(@resource.default_locale)
    contact_email = project.contact_email
  else # if no project or more than one
    project_name = 'OHD'
    # TODO: change this email
    contact_email = "mail@oral-history.digital"
  end

  case my_locale.to_sym
  when :en
-%>
Dear <%= @user.display_name %>,

Your access to the application "<%= project_name %>" has been blocked.

This happens if you have violated the Terms of Use.

If you have any questions regarding this procedure, please contact us at <%= contact_email %>.

Yours sincerely,

The project team "<%= project_name %>"
<% when :el -%>
Αγαπητή/-έ <%= @user.display_name %>,

Η πρόσβαση σας στην εφαρμογή "<%= project_name %>" αποκλείστηκε.

Αυτό συμβαίνει αν παραβιάσατε τους Όροι Χρήσης.

Σε περίπτωση που έχετε οποιαδήποτε απορία μπορείτε να επικοινωνήσετε μαζί μας στη διεύθυνση <%= contact_email %>.

Με εκτίμηση,

Η ομάδα του προγράμματος "<%= project_name %>"
<% when :es -%>
Estimada/o <%= @user.display_name %>,

Su acceso a la aplicación "<%= project_name %>" ha sido bloqueado.

Eso sucede si ha violado los Términos de uso.

Si tiene preguntas sobre el proceso, por favor contáctenos bajo <%= contact_email %>.

Con saludos cordiales,

El equipo del proyecto "<%= project_name %>"
<% when :ru -%>
Уважаемый(-ая) <%= @user.display_name %>,

Ваш доступ к ресурсу "<%= project_name %>" был заблокирован.

Это произошло, если Вы нарушили Условия пользовния.

Если у Вас в связи с этим возникли вопросы, пожалуйста, свяжитесь с нами: <%= contact_email %>.

С уважением,

сотрудники проекта "<%= project_name %>"
<% else -%>
Guten Tag <%= @user.display_name %>,

hiermit teilen wir Ihnen mit, dass Ihr Zugang zu der Plattform Oral-History.Digital gesperrt wurde. Dies erfolgt, wenn die Nutzungsbedingungen der Plattform (<%= @conditions_link.url(:de) %>) nicht beachtet wurden.
Bei Rückfragen kontaktieren Sie uns bitte unter mail@oral-history.digital.

<% unless @user.admin_comments.blank? %>
  <%= @user.admin_comments %>
<% end %>

Mit freundlichen Grüßen

Oral-History. Digital
<% end -%>
