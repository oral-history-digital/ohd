<% case @resource.default_locale.to_sym || I18n.locale
   when :en -%>
Hello <%= @resource.display_name %>,

You have requested a one-time code for the Oral-History.Digital platform.

<%= @code %>

The code is valid for <%= @valid_for %> minutes.

With best regards

Oral-History. Digital
<% when :ru -%>
Добрый день <%= @resource.display_name %>,

Вы запросили одноразовый код для платформы Oral-History.Digital.

<%= @code %>

Код действителен в течение <%= @valid_for %> минут.

С уважением,

Oral-History.Digital
<% when :uk -%>
Доброго дня<%= @resource.display_name %>,

Ви подали запит на отримання одноразового коду для платформи Oral-History.Digital.

<%= @code %>

Код дійсний протягом <%= @valid_for %> хвилин.

З найкращими побажаннями

Oral-History.Digital
<% when :el -%>
Γεια σας <%= @resource.display_name %>,

Ζητήσατε έναν κωδικό μίας χρήσης για την πλατφόρμα Oral-History.Digital.

<%= @code %>

Ο κωδικός ισχύει για <%= @valid_for %> λεπτά.

Με εκτίμηση,

Oral-History.Digital

<% when :es -%>
Hola <%= @resource.display_name %>,

Ha solicitado un código de un solo uso para la plataforma Oral-History.Digital.

<%= @code %>

El código es válido por <%= @valid_for %> minutos.

Saludos cordiales,

Oral-History.Digital

<% when :ar -%>
  مرحبًا <%= @resource.display_name %>,

  لقد طلبت رمزًا لمرة واحدة لمنصة Oral-History.Digital.
  
  <%= @code %>
  
  الرمز صالح لمدة <%= @valid_for %> دقائق.

  مع أطيب التحيات

  Oral-History.Digital

<% else -%>
Guten Tag <%= @resource.display_name %>,

ie haben einen Einmal-Code für die Plattform Oral-History.Digital angefordert.

<%= @code %>

Der Code ist für <%= @valid_for %> Minuten gültig.

Mit freundlichen Grüßen,

Oral-History.Digital 
<% end -%>
